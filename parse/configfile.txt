# This is a comment explaining what the following section does

# Set the server's default behavior

server {

    listen 8081
    server_name example1.com
    host  127.0.0.1
    

    root /Users/nachab/Desktop/latestWebServ/

    index index.html
max_body_size   1000000000
    # Configure error pages
    error_page 404 /cgi/

    location /image/ {
        index index.html
        methods GET POST
        cgi_extention   .php
        error_page 404 /cgi/
        max_body_size   1000000000
    }
    location / {
        root /Users/nachab/Desktop/latestWebServ/
        root /Users/nachab/Desktop/latestWebServ/
        index index.html
        methods GET POST
    }

    location /txt/ {

        root slm2
        index slm3
        methods POST
        autoindex off
        cgi_extention   .dd
        cgi_bin      dff
    }
}
